#include <stdio.h>
int main() {
  char a[15], mexico[15] = "Mexico", usa[15] = "USA", canada[15] = "Canada";
     printf("Enter country: ");
  int m = 1, u = 1, c = 1;
  gets(a);//gets помещает введенную строку в переменную (a), затем выводит ее значение на экран
  for (int i = 0; i < 6; i++) {
    if (a[i] != mexico[i]) {
      m = 0;
    }
  }
  for (int i = 0; i < 3; i++) {
    if (a[i] != usa[i]) {
      u = 0;
    }
  }
  for (int i = 0; i < 6; i++) {
    if (a[i] != canada[i]) {
      c = 0;
    }
  }
  if (c != 1 && u != 1 && m != 1)
    printf("Unknown continent");
  else
    printf("North America");
  return 0;
}
